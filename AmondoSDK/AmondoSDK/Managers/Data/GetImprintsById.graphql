query GetImprintsById($ids: [ID]) {
    imprints(where: {ids: $ids}) {
        id
        artist
        startDate
        endDate
        createdAt
        updatedAt
        location
        image {
          url
          type
          width
          height
          publicId
          format
          version
        }
    }
}
