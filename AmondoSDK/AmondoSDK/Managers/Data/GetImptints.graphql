query GetImprints($first: Int, $skip: Int) {
  imprints(first: $first, skip: $skip) {
    id
    publicId
    artist
    startDate
    endDate
    createdAt
    updatedAt
    location
    image {
      url
      type
      width
      height
      publicId
      format
      version
    }
  }
}
